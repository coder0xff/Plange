return {
	mean <- (<FiniteDenumerable<T>> values) {
		l := values.size;
		
	}

	<Real -> Real> arctan <-
		(<Float32> v) {
			return CStdLib.atanf(v);
		}
	|
		(<Float64> v) {
			return CStdLib.atan(v);
		}
	;

};
